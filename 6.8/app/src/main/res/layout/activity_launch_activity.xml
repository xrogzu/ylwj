<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/grey"
    tools:context="com.administrator.fragment.Launch_activityFragment">

    <ScrollView
        android:id="@+id/scrollview_activity"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:scrollbars="none"
        android:layout_weight="1">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="10dp"
            android:focusable="true"
            android:focusableInTouchMode="true"
            android:orientation="vertical">

            <include layout="@layout/launch_activity_header"/>

            <FrameLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@color/white"
                android:layout_marginBottom="20dp"
                android:id="@+id/container_framelayout">
            </FrameLayout>
        </LinearLayout>

    </ScrollView>
    <Button
        android:id="@+id/bt_next_step"
        android:background="@color/orangered"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:text="下一步"
        android:textSize="@dimen/text_login"
        android:textColor="@color/white"
        android:textStyle="bold" />

    <Button
        android:visibility="gone"
        android:id="@+id/bt_login"
        android:layout_gravity="center"
        android:gravity="center"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@mipmap/unlogin"
        android:layout_marginTop="2dp"
        />
</LinearLayout>
